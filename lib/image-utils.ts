
export function getImageUrl(imagePath: string, imagekitPath?: string | null): string {
  // Always use the local path and ignore ImageKit path
  return imagePath || '';
}

export function getThumbnailUrl(imagePath: string, imagekitPath?: string | null): string {
  // Always use the local path and ignore ImageKit path
  return imagePath || '';
}

export function getFullSizeUrl(imagePath: string, imagekitPath?: string | null): string {
  // Always use the local path and ignore ImageKit path
  return imagePath || '';
}
